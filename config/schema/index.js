import { archive } from "./archive";
import { build } from "./build";
import { cfg } from "./cfg";
import { core } from "./core";

export const schema = {
    archive,
    build,
    cfg,
    core,
};
