import { APW } from "../../APW";
import { FileType as ApwFileType } from "../../@types/FileType";

export const FileType = {
    apw: ApwFileType,
    script: "Script",
    env: "Env",
};

export default FileType;
